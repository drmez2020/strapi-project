'use strict';

/**
 * regestration-request router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::regestration-request.regestration-request');
