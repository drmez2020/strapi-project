'use strict';

/**
 * regestration-request service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::regestration-request.regestration-request');
